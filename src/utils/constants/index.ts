import theme from "../../theme";
import { TVectorColors } from "../../types/vectors";

export const DEFAULT_VECTOR_COLORS: TVectorColors = {
  dark: theme.colorsPalette.blue.dark,
  default: theme.colorsPalette.blue.default,
  light: theme.colorsPalette.blue.light,
};
