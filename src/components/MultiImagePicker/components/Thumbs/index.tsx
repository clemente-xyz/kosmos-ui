import styled from 'styled-components';

const MultiImagePickerThumbs = styled.aside`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  margin-top: 16px;
`;

export default MultiImagePickerThumbs;
