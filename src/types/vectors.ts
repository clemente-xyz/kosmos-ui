import { CSSProperties } from "react";

export interface IVectorProps {
  height?: string;
  color?: string;
  primaryColor?: string;
  secondaryColor?: string;
  style?: CSSProperties;
  className?: string;
}
